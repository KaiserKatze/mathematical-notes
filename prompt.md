尝试证明或证伪以下命题：
```latex
% TODO
设\(\hat{\vb\theta}\)是向量\(\vb\theta\)的无偏估计量，
则\(\hat{\vb\theta}\)关于\(\vb\theta\)的均方误差\(M(\hat{\vb\theta})\)
等于\(\hat{\vb\theta}\)的协方差矩阵的迹.
```

输出结果应该表示为LaTeX代码，不应该表示为Markdown代码。
输出结果需要放在代码块中。
不要添加引言区（不可以调用 \documentclass,\usepackage 等命令）、命令定义（不可以调用 \def,\edef,\NewDocumentCommand,\DeclareMathOperator 等命令）；
只输出与题目复述、证明、证伪、计算相关的LaTeX代码。
文本（非公式）用中文叙述。
当需要通过给出具体例子进行证明或证伪时，
对于涉及数值计算的步骤，你应该利用 numpy、scipy 等工具，实现 python 脚本，借助 ai agent 进行计算；
对于涉及符号计算的步骤，你应该设计 Mathematica 代码。
python 代码和 Mathematica 代码放在单独的代码框中进行展示。
如果上述命题是假命题，那么需要提出具体例子。
使用 `\(` 和 `\)` 作为行内公式定界符。行内公式定界符前后不要增加空格。
使用加粗大小拉丁字母 `\vb{A}` 表示名为 `A` 的矩阵。
使用加粗小写希腊字母 `\vb{\alpha}` 表示名为 `\alpha` 的向量。
用 `\mathbb{R},\mathbb{C}` 表示实数域、复数域。
用 `f\colon X \to Y` 表示“从集合X到集合Y的映射”，仅保证单值性，不保证满射性、单射性。
用 `\abs{x}` 表示实数 x 的绝对值。
用 `\ComplexConjugate{z}` 表示复数 z 的共轭复数。
用 `\ComplexLengthA{z}` 表示复数 z 的绝对值。
绝对不要直接用mathjax等javascript库渲染数学公式。
使用 `\begin{align*}` 和 `\end{align*}` 作为行间公式环境。
在 `align*` 环境中，每行公式最多使用一次 `&` 符号用来对齐。
在 `align*` 环境中，如果需要注明用来推导相邻两行公式的依据（公理、定理、推论等），那么在行尾换行前使用 `\tag{reason}` 进行说明。
在行间公式中表示向量、矩阵时，使用 `\begin{bmatrix} \end{bmatrix}` 环境。
行间公式的最后一行必须加上逗号、分号或句号，具体根据上下文语义决定。
“即”字之前的最后一句文本或最后一行公式（这时 `\end{align*}` 不计入行数）必须以逗号结尾。
环境内的每一行都要缩进4个空格（相对于上一级环境），而 `\begin{...}` 和 `\end{...}` 相对于上一级环境不缩进。
用英文半角句号代替中文全角句号。
文本模式下，如果一行文字超过10个字，则在句号、中文全角逗号后换行。
用 `A^T` 表示矩阵$A$的转置，用 `A^H` 表示矩阵$A$的共轭转置。
用 `\tr A` 表示矩阵$A$ 的迹。
用 `\DeterminantA{A}` 表示方阵$A$的行列式；不要用 `\abs{A}` 或 `\det A` 表示行列式。
用 `E(X)` 表示随机变量 `X` 的数学期望。
用 `D(X)` 表示随机变量 `X` 的方差。
用 `\Cov(X,Y)` 表示随机变量 `X,Y` 的协方差。
用 `\Cov(\vb{X})` 表示随机向量 `\vb{X}` 的协方差矩阵。
