\part{线性代数}
\begingroup
\def\x{\mat{X}}
\def\y{\mat{Y}}
\def\a{\mat{\alpha}}
\def\b{\mat{\beta}}
\def\e{\mat{\varepsilon}}
\def\g{\mat{\gamma}}
\def\X#1{\x_{#1}}
\def\A{\mat{A}}
\def\B{\mat{B}}
\def\C{\mat{C}}
\def\D{\mat{D}}
\def\P{\mat{P}}
\def\Q{\mat{Q}}
\def\l{\lambda}
\def\L#1{\l_{#1}}
\def\z{\mat{0}}
\def\E{\mat{E}}
\def\V{\mat{\Lambda}}

%线性代数的核心内容是研究有限维线性空间的结构和线性空间的线性变换.
%由于数域\(P\)上的任意一个\(n\)维线性空间\(V\)都与\(n\)维向量空间\(P^n\)同构，
%\(V\)上的全体线性变换构成的集合\(L(V,V)\)%
%与数域\(P\)上的全体\(n \times n\)矩阵构成的集合\(P^{n \times n}\)同构，
%因此，在本书中线性代数主要研究：
%矩阵理论、\(n\)维向量的线性关系、线性方程组、行列式、二次型、矩阵的特征值与特征向量、内积等内容.

\input{线性代数/线性方程组}%向量与线性方程组
\input{线性代数/行列式}%行列式
\input{线性代数/向量空间}%
%\input{线性代数/向量与矩阵}%向量、矩阵及其基本运算
\input{线性代数/逆矩阵}%矩阵的逆
\input{线性代数/特征值与特征向量}%特征值、特征向量
\input{线性代数/二次型}%二次型
%\input{线性代数/线性空间}%线性空间与线性变换
\endgroup
