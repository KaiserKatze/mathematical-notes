%\section{集合的大小}
%\begin{property}
%设\(A\)是有限集.
%证明：\(A\)的幂集的基数为\[
%	\abs{\Powerset A} = 2^{\abs{A}}.
%\]
%%TODO
%%\begin{proof}
%%\end{proof}
%\end{property}
%
%\begin{example}
%设\(A\)、\(B\)是任意两个集合，试证：
%\(\abs{A \cup B} + \abs{A \cap B} = \abs{A} + \abs{B}\).
%%TODO
%\end{example}
